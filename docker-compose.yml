version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - sqlite_data:/app/instance
    env_file:
      - .env
    restart: always

volumes:
  sqlite_data: