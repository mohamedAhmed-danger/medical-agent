version: '3.8'
services:
  web:
    build: .
    container_name: medical-agent-markting
    ports:
      - "127.0.0.1:2306:2306"
    volumes:
      - sqlite_data:/app/instance
    env_file:
      - .env
    restart: always
volumes:
  sqlite_data: